spring:
  application:
    name: Owner-microservice

server:
  port: 8085

eureka:
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://localhost:8080/eureka/
  instance:
    hostname: localhost
    command:
    default:
      execution:
        isolation:
          thread:
            timeoutInMilliseconds: 60000