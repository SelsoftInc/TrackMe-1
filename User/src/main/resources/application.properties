#mongodb
spring.data.mongodb.host=localhost
spring.data.mongodb.port=27017
spring.data.mongodb.database=TrackMeDev
spring.data.mongodb.username=trackme-dev-user
spring.data.mongodb.password=trackmeDevUser
spring.data.mongodb.authenticationDatabase=user-data
spring.data.mongodb.uri=66.175.215.173


jwt:
  header: Authorization
  secret: mySecret
  expiration: 604800
  route:
    authentication:
      path: auth
      refresh: refresh
      token: check
logging:
  level:
    org.springframework.security: DEBUG
server:
  port: 8080
spring:
  resources:
    chain:
      enabled: true
management:
  security:
    enabled: true
    